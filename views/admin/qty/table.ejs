<table id="example2" class="table table-bordered table-hover table-striped" style="width: 100%">
  <thead>
    <tr>
      <th style="width: 7%">No</th>
      <th>Nama qty</th>
      <th>jumlah</th>
      <th style="width: 20%">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% for(let i = 0; i < qty.length; i++){ %>
    <tr>
      <td><%= i + 1 %></td>
      <td><%= qty[i].nama %></td>
      <td><%= qty[i].category_qty %></td>
      <td>
        <form action="/qty/delete/<%= qty[i]._id %>?_method=DELETE" method="POST">
          <a href="/qty/edit/<%= qty[i]._id %>" class="btn btn-warning">
            <i class="fas fa-pencil-alt"></i>
            Edit
          </a>
          <button href="" class="btn btn-danger">
            <i class="fas fa-trash-alt"></i>
            Delete
          </button>
        </form>
      </td>
    </tr>
    <% } %>
  </tbody>
</table>
