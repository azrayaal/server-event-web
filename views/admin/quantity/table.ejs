<table id="example2" class="table table-bordered table-hover table-striped" style="width: 100%">
  <thead>
    <tr>
      <th style="width: 7%">No</th>
      <th>Nama qty</th>
      <th>jumlah</th>
      <th style="width: 20%">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% for(let i = 0; i < quantity.length; i++){ %>
    <tr>
      <td><%= i + 1 %></td>
      <td><%= quantity[i].nama %></td>
      <td><%= quantity[i].quantity %></td>
      <td>
        <form action="/quantity/delete/<%= quantity[i]._id %>?_method=DELETE" method="POST">
          <a href="/quantity/edit/<%= quantity[i]._id %>" class="btn btn-warning">
            <i class="fas fa-pencil-alt"></i>
            Edit
          </a>
          <button href="" class="btn btn-danger">
            <i class="fas fa-trash-alt"></i>
            Delete
          </button>
        </form>
      </td>
    </tr>
    <% } %>
  </tbody>
</table>
